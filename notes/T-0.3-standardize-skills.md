# T-0.3 — Standardize skills with creating-skills blueprint
_2026-07-20 · outcome: DONE_

## What I did
- Renamed skill directories under `.agents/skills/` to use `snake_case` conventions (`adding_eval_cases`, `adding_loop_exit_checkers`, `adding_or_changing_contracts`, `running_testmode_pipeline`, `scaffolding_specialists`).
- Standardized YAML frontmatter using gerund naming (`kebab-case`), action-oriented routing descriptions with definitive out-of-scope triggers, tool requirements, and author metadata.
- Structured all skill markdown files to adhere strictly to the `creating_skills` template (`When to use`, `When NOT to use`, `Workflow`, `Examples`, `Output format`, and `Anti-patterns to avoid`).
- Updated cross-references across `creating_skills/SKILL.md` and `IMPLEMENTATION_PLAN.md` to point to the renamed skills and directories.

## Issues faced & how I fixed them
- None. All skill structures cleanly transitioned to the new schema and conventions.

## Deviations from PDD / this plan
- None.

## Rules / skills feedback
- The `creating_skills` blueprint effectively enforces clear routing boundaries and single-responsibility structures across all agent skills.

## Follow-ups for later tasks
- Continue applying the `creating_skills` check and template whenever introducing new skills or procedures in subsequent milestones.
