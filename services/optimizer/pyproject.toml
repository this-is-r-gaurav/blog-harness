[project]
name = "optimizer"
version = "0.1.0"
description = "Offline optimizer service"
requires-python = ">=3.12"
dependencies = [
    "blog-contracts",
    "blog-platform",
    "blog-observability",
]

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"
