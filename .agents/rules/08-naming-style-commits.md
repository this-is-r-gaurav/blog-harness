# Naming, style, and commits
1. FORMATTING — Use `ruff` for formatting.
   CHECK: Ensure CI runs `ruff`.
2. CONVENTIONAL COMMITS — Use conventional commit messages.
   CHECK: Review commit messages.
3. PR MUST CITE PDD § — Pull requests must explicitly cite the PDD sections they satisfy.
   CHECK: Review PR templates and descriptions.
