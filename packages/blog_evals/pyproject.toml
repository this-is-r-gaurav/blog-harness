[project]
name = "blog-evals"
version = "0.1.0"
description = "Evaluation models, fixtures, and harnesses"
requires-python = ">=3.12"
dependencies = [
    "blog-contracts",
]

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"
