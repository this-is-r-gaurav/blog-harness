[project]
name = "blog-agent-kit"
version = "0.1.0"
description = "Agent kit and builder for blog harness specialists"
requires-python = ">=3.12"
dependencies = [
    "blog-contracts",
    "blog-observability",
]

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"
