[project]
name = "blog-contracts"
version = "0.1.0"
description = "Shared contract models and schemas"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"
