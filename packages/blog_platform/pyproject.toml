[project]
name = "blog-platform"
version = "0.1.0"
description = "Platform ports, GCP adapters, TEST_MODE fakes, and DI factory"
requires-python = ">=3.12"
dependencies = [
    "blog-contracts",
    "pydantic>=2.0.0",
]

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"
